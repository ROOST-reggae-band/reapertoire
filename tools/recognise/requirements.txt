numpy==1.26.4
# numpy alone. librosa's beat tracker and CQT are numba-compiled on first call,
# which cost nineteen seconds before a single take was analysed. Decoding is
# ffmpeg's job, and everything else here is an FFT and an autocorrelation.
